#include "main.h"
#include<stdio.h>
/**
 * puts - outputs string
 * @str: pointer to string
 * * main - Entry point
 * Return: Always 0 (Success)
 */
void _puts(char *str);
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
}
